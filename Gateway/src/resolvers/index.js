import Query from './query/index';
import Mutation from './mutation/index';
// ------------------------------------------------------------------------------------------------
const resolvers = {
	Query,
	Mutation
};

export default resolvers;
